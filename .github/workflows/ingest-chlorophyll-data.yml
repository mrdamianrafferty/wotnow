name: Ingest Chlorophyll Data

on:
  schedule:
    # Run every 6 hours (chlorophyll is monthly composite, doesn't need hourly updates)
    - cron: '30 */6 * * *'
  workflow_dispatch:
    inputs:
      region:
        description: 'Region to ingest'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - california
          - florida
          - newyork
          - gulf
          - pacific-nw
          - hawaii
          - alaska
          - canada-west
          - canada-east
      limit:
        description: 'Max cells per region'
        required: false
        default: '50'
        type: string

jobs:
  ingest-chlorophyll:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up environment
        run: |
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" >> .env.local
          echo "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" >> .env.local

      - name: Ingest California (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'california' || github.event_name == 'schedule'
        run: |
          echo "🌊 Ingesting California chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --california --limit=50

      - name: Ingest Florida (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'florida' || github.event_name == 'schedule'
        run: |
          echo "🌊 Ingesting Florida chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --florida --limit=50

      - name: Ingest New York (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'newyork' || github.event_name == 'schedule'
        run: |
          echo "🌊 Ingesting New York chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --newyork --limit=50

      - name: Ingest Gulf of Mexico (75 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'gulf'
        run: |
          echo "🌊 Ingesting Gulf of Mexico chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --gulf --limit=75

      - name: Ingest Pacific Northwest (75 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'pacific-nw'
        run: |
          echo "🌊 Ingesting Pacific Northwest chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --pacific-nw --limit=75

      - name: Ingest Hawaii (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'hawaii'
        run: |
          echo "🌊 Ingesting Hawaii chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --hawaii --limit=50

      - name: Ingest Alaska (75 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'alaska'
        run: |
          echo "🌊 Ingesting Alaska chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --alaska --limit=75

      - name: Ingest Canada West Coast (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'canada-west'
        run: |
          echo "🌊 Ingesting Canada West Coast chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --canada-west --limit=50

      - name: Ingest Canada East Coast (50 grids)
        if: github.event.inputs.region == 'all' || github.event.inputs.region == 'canada-east'
        run: |
          echo "🌊 Ingesting Canada East Coast chlorophyll..."
          npx tsx scripts/test-chlorophyll-ingestion.ts --canada-east --limit=50

      - name: Check coverage
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "📊 Checking chlorophyll data coverage..."
          npx tsx -e "(async () => { const { createClient } = await import('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { count: totalCount } = await supabase.from('grid_conditions_latest').select('*', { count: 'exact', head: true }); const { count: chlCount } = await supabase.from('grid_conditions_latest').select('*', { count: 'exact', head: true }).contains('sources', ['erdMH1chlamday.chlorophyll']); const { count: nonNullCount } = await supabase.from('grid_conditions_latest').select('*', { count: 'exact', head: true }).not('chlorophyll_mg_m3', 'is', null); console.log('📊 Chlorophyll Data Coverage:'); console.log('  Total grids with any data: ' + totalCount + ' / 65,884'); console.log('  Grids with chlorophyll source: ' + chlCount); console.log('  Grids with non-null chlorophyll: ' + nonNullCount); console.log('  Coverage: ' + ((chlCount/65884*100).toFixed(2)) + '%'); })()"

      - name: Report status
        if: always()
        run: |
          echo "Job status: ${{ job.status }}"
          if [ "${{ job.status }}" = "success" ]; then
            echo "✅ Chlorophyll data ingestion workflow completed successfully"
          else
            echo "❌ Chlorophyll data ingestion workflow encountered failures. Check logs above for details."
          fi
          echo "See coverage summary above for current status"

