name: Refresh Materialized Views

on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
  workflow_dispatch:  # Allow manual trigger

concurrency:
  group: refresh-materialized-views
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'mrdamianrafferty/wotnow' }}
    timeout-minutes: 5
    steps:
      - name: Refresh findr_conditions_latest
        run: |
          curl -sf -X POST \
            "${{ secrets.SUPABASE_URL }}/rest/v1/rpc/refresh_findr_conditions_latest" \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
          echo "✅ findr_conditions_latest refreshed"
